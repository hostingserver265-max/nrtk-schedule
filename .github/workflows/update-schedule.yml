name: Update Schedule

# Когда запускать
on:
  schedule:
    # Каждый день в 07:00 и 13:00 по Москве (04:00 и 10:00 UTC)
    - cron: '0 4,10 * * *'

  # Можно запустить вручную кнопку
  workflow_dispatch:

  # При пуше в main тоже запускается (для теста)
  push:
    branches: [ main ]

# Права на запись в репозиторий (важно для git push)
permissions:
  contents: write

jobs:
  update-schedule:
    runs-on: ubuntu-latest

    steps:
    # Шаг 1: Клонируем репозиторий
    - name: 📦 Checkout repository
      uses: actions/checkout@v3

    # Шаг 2: Устанавливаем Python 3.10
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    # Шаг 3: Устанавливаем зависимости
    - name: 📚 Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    # Шаг 4: Удаляем старый файл и запускаем парсер
    - name: 🚀 Parse schedule
      run: |
        # Удаляем старый файл, если он есть
        rm -f schedule.json
        echo "🗑️ Старый файл schedule.json удален (если был)"
        
        # Запускаем скрипт, он создаст новый файл с нуля
        python parse_schedule.py

    # Шаг 5: Коммитим изменения (ИСПРАВЛЕНО)
    - name: 💾 Commit and push changes
      run: |
        git config --global user.name 'Schedule Bot'
        git config --global user.email 'bot@nrtk.schedule'
        
        # ПРОВЕРКА: Существует ли файл?
        if [ -f schedule.json ]; then
          git add schedule.json
          
          # Проверяем, есть ли реальные изменения
          if git diff --staged --quiet; then
            echo "✅ Изменений нет, расписание такое же, как было."
          else
            git commit -m "🔄 Обновление расписания $(date +'%Y-%m-%d %H:%M')"
            
            # --- ВАЖНОЕ ИСПРАВЛЕНИЕ НИЖЕ ---
            # Сначала подтягиваем изменения с сервера, чтобы избежать конфликта
            git pull origin main --rebase
            
            # Теперь отправляем
            git push origin main
            echo "✅ Ура! Расписание обновлено и о
